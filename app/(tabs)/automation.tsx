import { StubScreen } from '@/components/StubScreen';

export default function AutomationPlaceholder() {
  return <StubScreen title="Automation" subtitle="Bots, branching, and scripted responses." />;
}
